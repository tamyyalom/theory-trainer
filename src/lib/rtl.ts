/** Glyphs and labels for Hebrew RTL navigation */
export const ARROW_BACK = '→'
export const ARROW_FORWARD = '←'

export function labelBack(text: string): string {
  return `${ARROW_BACK} ${text}`
}
